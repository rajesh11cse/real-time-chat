# nginx configuration for load balancing and WebSocket proxying
worker_processes  1;

events {
  worker_connections  1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  sendfile        on;
  keepalive_timeout  65;

  upstream user_service_upstream {
    server user-service-1:4001;
    server user-service-2:4001;
  }

  # ip_hash so same client hits same chat instance (helps subscription consistency)
  upstream chat_service_upstream {
    ip_hash;
    server chat-service-1:4002;
    server chat-service-2:4002;
  }

  upstream frontend_upstream {
    server frontend:3000;
  }

  map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
  }

  server {
    listen 80;

    # Frontend
    location / {
      proxy_pass http://frontend_upstream;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # User-service GraphQL (HTTP + WebSocket)
    location /api/user/graphql {
      # rewrite: external path -> internal /graphql
      proxy_pass http://user_service_upstream/graphql;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Chat-service GraphQL (HTTP + WebSocket) - long timeouts for subscriptions
    location /api/chat/graphql {
      proxy_pass http://chat_service_upstream/graphql;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
      proxy_connect_timeout 7s;
    }
  }
}

